# Configuration

Templates in this directory are safe defaults and examples, not deployable secrets. Copy them into the platform configuration system and inject environment-specific values at deployment time.
