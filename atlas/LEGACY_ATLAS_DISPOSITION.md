# Legacy Atlas Disposition

## Status

`ARCHIVE_REFERENCE / NONCANONICAL`

## Scope

This classification applies to legacy ChatGPT Library Atlas documents that use identifiers such as `ATLAS-000`, `ATLAS-006`, `ATLAS-008`, and `ATLAS-020`, including files whose internal metadata claims `CANONICAL`.

## Decision

Those files are historical/reference inputs only. They are not part of the current `atlas/DOCUMENT_REGISTRY.csv`, which contains 173 `pending` records using domain IDs such as `GOV-*`, `FND-*`, `PRD-*`, and `ADR-*`.

The legacy files:

- do not govern Repository A;
- do not override the canonical Product hierarchy;
- do not replace current decision IDs, status vocabularies, paths, or ownership;
- do not authorize Product/Knowledge generation, WordPress, runtime, import, publishing, deployment, production, or Phase 1 AI;
- may be adopted only through a later path-level mapping and explicit Founder approval.

Their original content and chronology should be preserved. K-01 performs classification only and does not move, delete, rewrite, or promote the Library files.

## Controlling References

- `docs/CURRENT_PROJECT_STATE.md`
- `docs/KNOWLEDGE_ARCHIVE_STANDARD.md`
- `atlas/DOCUMENT_REGISTRY.csv`
- `atlas/ATLAS_ADOPTION_MATRIX.csv`
- `docs/17_FOUNDER_DECISION_LOG.md` (`FD-K01-001`)
