# C004 competitive-intelligence fixtures

The canonical registries are the positive package. The valid synthetic file is a
non-authoritative pointer proving the bounded shape. The mutation manifest drives
semantic fail-closed tests; the adversarial fixtures prove duplicate-key,
permissive-schema and remote-reference rejection. No fixture contains Product,
SKU, Availability, current/public price or Runtime authority.
