# C009-FT2 validation fixtures

These fixtures exercise the append-only post-C009 gate re-evaluation delta.
The synthetic positive surface preserves the same `FALSE / 5 of 12` semantics
but has a distinct fixture mode and evaluation time. Mutation and adversarial
fixtures must fail closed; none creates Product, commercial, Runtime, Merge,
M4, or successor authority.
