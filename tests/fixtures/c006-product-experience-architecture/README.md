# C006 adversarial fixtures

These offline fixtures test duplicate-key rejection, local-only and recursively closed
schema enforcement, resource caps, and the deterministic mutation dispatch defined in
`mutation-cases.json`. They create no Product, Variant, SKU, Availability, price, media,
Runtime, or Production facts.
